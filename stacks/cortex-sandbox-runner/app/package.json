{
  "name": "cortex-sandbox-runner",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "description": "gVisor-backed Express runner. POST /exec spawns short-lived podman --runtime=runsc containers and returns captured stdio.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test \"../tests/**/*.test.js\""
  },
  "dependencies": {
    "@cortexos/telemetry": "*",
    "express": "^4.21.2",
    "zod": "^3.23.8"
  }
}
