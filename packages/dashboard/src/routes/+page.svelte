<script lang="ts">
	// Root page — the server always redirects to /dashboard or /login.
	// This template only exists so SvelteKit can serve a `/` route while
	// the redirect happens server-side.
</script>

<p class="sr-only">Loading CortexOS…</p>
