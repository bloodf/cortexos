# Implementation Plans

Generated by the improve skill on 2026-06-19 at commit `432f058c`. Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor STOP conditions, and update your row when done.

## Audit source

- Recon and audit followed `skill://improve/references/audit-playbook.md`.
- Plans use repo evidence from `README.md`, `AGENTS.md`, `packages/dashboard-next/AGENTS.md`, `.github/workflows/ci.yml`, package manifests, `pnpm-lock.yaml`, and `packages/dashboard-next/src/lib/api/headroom.functions.ts`.
- `pnpm audit --prod --audit-level=high` was run during audit and failed with three high advisories: `form-data`, `vite`, and `undici`.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|---|---|---|---|---|---|
| [001](001-make-ci-gates-truthful.md) | Make CI gates truthful and remove stale dashboard-compose checks | P1 | S | — | DONE |
# Implementation Plans

Generated by the improve skill on 2026-06-19 at commit `432f058c`. Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor STOP conditions, and update your row when done.

## Audit source

- Recon and audit followed `skill://improve/references/audit-playbook.md`.
- Plans use repo evidence from `README.md`, `AGENTS.md`, `packages/dashboard-next/AGENTS.md`, `.github/workflows/ci.yml`, package manifests, `pnpm-lock.yaml`, and `packages/dashboard-next/src/lib/api/headroom.functions.ts`.
- `pnpm audit --prod --audit-level=high` was run during audit and failed with three high advisories: `form-data`, `vite`, and `undici`.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|---|---|---|---|---|---|
| [001](001-make-ci-gates-truthful.md) | Make CI gates truthful and remove stale dashboard-compose checks | P1 | S | — | DONE |
| [002](002-clear-high-production-advisories.md) | Clear high-severity production dependency advisories | P1 | S | — | DONE |
| [003](003-fix-headroom-dashboard-url.md) | Fix Headroom dashboard URL construction | P2 | S | — | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale).

## Dependency notes

- 001 and 002 are independent P1 release-health fixes.
- 003 is independent and can run anytime after current dashboard-next code is still at the cited Headroom implementation.

## Deferred findings

- Real a11y and contract gates: CI currently has placeholder `echo` jobs. Plan 001 removes false gates; a follow-up should add actual `test:a11y` and contract scripts/suites before making them required again.
- Toolchain drift: root `package.json` says `pnpm@10.12.1`, CI/docs say pnpm 11 and Node 24. Needs a separate toolchain-alignment plan after CI is truthful.
- Broader Headroom endpoint config: `getHeadroomHealth` and `getHeadroomStats` still use hardcoded `http://127.0.0.1:8787`. Plan 003 fixes only the malformed dashboard URL; a later plan can introduce a documented `HEADROOM_URL` if operators need non-local Headroom API targets.
- Direction audit: grounded roadmap items were not planned in this pass because release-health and correctness findings had higher leverage.

## Findings considered and rejected

- Reintroduce `packages/dashboard/docker-compose.yml` to satisfy CI: rejected because `packages/dashboard-next/AGENTS.md` explicitly says dashboard-next has no container, Docker Compose, or image deploy path.
- Raise coverage thresholds directly to 95% as part of Plan 001 unless executor chooses that path and tests pass; truthful wording is the safer default.
| [003](003-fix-headroom-dashboard-url.md) | Fix Headroom dashboard URL construction | P2 | S | — | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale).

## Dependency notes

- 001 and 002 are independent P1 release-health fixes.
- 003 is independent and can run anytime after current dashboard-next code is still at the cited Headroom implementation.

## Deferred findings

- Real a11y and contract gates: CI currently has placeholder `echo` jobs. Plan 001 removes false gates; a follow-up should add actual `test:a11y` and contract scripts/suites before making them required again.
- Toolchain drift: root `package.json` says `pnpm@10.12.1`, CI/docs say pnpm 11 and Node 24. Needs a separate toolchain-alignment plan after CI is truthful.
- Broader Headroom endpoint config: `getHeadroomHealth` and `getHeadroomStats` still use hardcoded `http://127.0.0.1:8787`. Plan 003 fixes only the malformed dashboard URL; a later plan can introduce a documented `HEADROOM_URL` if operators need non-local Headroom API targets.
- Direction audit: grounded roadmap items were not planned in this pass because release-health and correctness findings had higher leverage.

## Findings considered and rejected

- Reintroduce `packages/dashboard/docker-compose.yml` to satisfy CI: rejected because `packages/dashboard-next/AGENTS.md` explicitly says dashboard-next has no container, Docker Compose, or image deploy path.
# Implementation Plans

Generated by the improve skill on 2026-06-19 at commit `432f058c`. Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor STOP conditions, and update your row when done.

## Audit source

- Recon and audit followed `skill://improve/references/audit-playbook.md`.
- Plans use repo evidence from `README.md`, `AGENTS.md`, `packages/dashboard-next/AGENTS.md`, `.github/workflows/ci.yml`, package manifests, `pnpm-lock.yaml`, and `packages/dashboard-next/src/lib/api/headroom.functions.ts`.
- `pnpm audit --prod --audit-level=high` was run during audit and failed with three high advisories: `form-data`, `vite`, and `undici`.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|---|---|---|---|---|---|
| [001](001-make-ci-gates-truthful.md) | Make CI gates truthful and remove stale dashboard-compose checks | P1 | S | — | DONE |
| [002](002-clear-high-production-advisories.md) | Clear high-severity production dependency advisories | P1 | S | — | DONE |
| [003](003-fix-headroom-dashboard-url.md) | Fix Headroom dashboard URL construction | P2 | S | — | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale).

## Dependency notes

- 001 and 002 are independent P1 release-health fixes.
- 003 is independent and can run anytime after current dashboard-next code is still at the cited Headroom implementation.

## Deferred findings

- Real a11y and contract gates: CI currently has placeholder `echo` jobs. Plan 001 removes false gates; a follow-up should add actual `test:a11y` and contract scripts/suites before making them required again.
- Toolchain drift: root `package.json` says `pnpm@10.12.1`, CI/docs say pnpm 11 and Node 24. Needs a separate toolchain-alignment plan after CI is truthful.
- Broader Headroom endpoint config: `getHeadroomHealth` and `getHeadroomStats` still use hardcoded `http://127.0.0.1:8787`. Plan 003 fixes only the malformed dashboard URL; a later plan can introduce a documented `HEADROOM_URL` if operators need non-local Headroom API targets.
- Direction audit: grounded roadmap items were not planned in this pass because release-health and correctness findings had higher leverage.

## Findings considered and rejected

- Reintroduce `packages/dashboard/docker-compose.yml` to satisfy CI: rejected because `packages/dashboard-next/AGENTS.md` explicitly says dashboard-next has no container, Docker Compose, or image deploy path.
- Raise coverage thresholds directly to 95% as part of Plan 001 unless executor chooses that path and tests pass; truthful wording is the safer default.
