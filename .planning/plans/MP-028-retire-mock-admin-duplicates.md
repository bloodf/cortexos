# MP-028 — retire the four MOCK admin duplicates (corrected scope)

Operator decision: "Retire the admin set" — recorded with the framing
CORRECTION (ledger): recon proved the nav-linked admin pages (Users,
Badges, EnvBrowser, Services, Account, Alerts, Projects) are LIVE
(features/admin/rpc.ts imports the real server fns:
listServices/createService/patchService/deleteService, readEnv/unlock,
me). Only FOUR pages are mock duplicates of live main pages, unlinked
in NavConfig.ts — its admin group is src/app/NavConfig.ts:98-104 and
lists ONLY /admin/services (:101), /admin/badges (:102),
/admin/env-browser (:103), /admin/users (:104); no docker/incus/
systemd/audit entries (re-verifiable via pre-proof 1b): admin/Docker.tsx,
admin/Incus.tsx, admin/Systemd.tsx, admin/Audit.tsx (each: runtime
`import { api } from "@/mocks/api"`).

## File ownership (NOTHING else; all under packages/dashboard-next/)
- DELETE src/features/admin/{Docker,Incus,Systemd,Audit}.tsx
- DELETE src/routes/_authenticated.admin.{docker,incus,systemd,audit}.tsx
- src/routeTree.gen.ts (regenerated by the build — commit the regen)
- CONDITIONAL (binary qualification): a test file qualifies for
  deletion ONLY if its import lines reference a deleted feature file —
  `grep -rln "from ['\"].*features/admin/\(Docker\|Incus\|Systemd\|Audit\)['\"]" packages/dashboard-next/src --include='*.test.*'`
  (quote-agnostic per critic cycle 2) — each match quoted, tagged
  [discovered-test:<path>], deleted in the same commit. A test that
  merely MENTIONS a deleted page without importing it does NOT qualify;
  if such a shared test would break, IMPL-BLOCKED. Nothing else.
- Report (HARNESS ARTIFACT, not repo file ownership — lives in
  gitignored .planning/, never committed, exempt from the
  "all under packages/dashboard-next/" boundary):
  artifacts/impl-mp-028-report.md

## Tasks
NOTE (TDD): classic RED does not apply — this is route/page DELETION,
not behavior change (MP-018 precedent). The PRE-PROOFS below are the
red-equivalent falsifiable checks; the gates after deletion are the
green proof.
1. PRE-PROOF (quote each, from /opt/cortexos):
   a. each of the four features imports mocks/api at runtime:
      `grep -n 'from "@/mocks/api"' packages/dashboard-next/src/features/admin/{Docker,Incus,Systemd,Audit}.tsx` → 4 hits;
   b. no nav/link references (CORRECTED 4th attempt: the old exclusion
      `grep -v 'routes/_authenticated.admin'` hid LIVE admin routes too —
      it missed projects.tsx's redirect to "/admin/incus", which broke tsc
      post-deletion on attempt 3; projects.tsx was retargeted to the live
      "/incus" pre-dispatch in c5148c2, like TopBar before it. Exclude ONLY
      the four routes being deleted plus the generated routeTree; the
      pattern is QUOTE-AGNOSTIC — single or double — per critic cycle 2;
      the routeTree exclusion is load-bearing: it holds 20 single-quoted
      '/admin/...' entries and is regenerated by the step-2 build):
      `grep -rn "['\"]/admin/\(docker\|incus\|systemd\|audit\)['\"]" packages/dashboard-next/src --include='*.ts*' | grep -v 'routes/_authenticated\.admin\.\(docker\|incus\|systemd\|audit\)\.tsx\|routeTree\.gen\.ts'` → empty;
   c. each route file imports ONLY its deleted feature:
      `grep -n 'import' packages/dashboard-next/src/routes/_authenticated.admin.{docker,incus,systemd,audit}.tsx` (quote);
   d. vitest BASELINE total:
      `bash -c 'set -a; source /opt/cortexos/.secrets/dashboard.env; set +a; cd packages/dashboard-next && pnpm exec vitest run 2>&1 | grep -E "Tests "'`
      → MUST show ZERO failed (`Tests  N passed (N)` with no `failed`
      token); quote the line — it is the Gates' pre-deletion total. A
      pre-existing failure (attempt-1 mode: `Tests  1 failed | 618
      passed (619)`) → IMPL-BLOCKED.
   e. GATES-AT-HEAD baseline (NEW 4th attempt — attempts 2-3 died on
      pre-existing HEAD failures the deletion did not cause; prove the
      gates are green BEFORE touching anything, from /opt/cortexos):
      `pnpm --filter @cortexos/dashboard-next exec tsc --noEmit; echo rc=$?` → rc=0;
      `pnpm run format:check >/dev/null 2>&1; echo rc=$?` → rc=0;
      `pnpm exec eslint . >/tmp/z.txt 2>/dev/null; echo rc=$?; wc -c < /tmp/z.txt` → rc=0, 0 bytes.
   If ANY proof fails → IMPL-BLOCKED with the evidence (do NOT fix
   pre-existing failures yourself — they are outside file ownership).
2. `git rm` the eight files; `pnpm --filter @cortexos/dashboard-next build`
   (regenerates routeTree) → exit 0; stage the regenerated
   routeTree.gen.ts.
3. Gates (binary, quote each; from /opt/cortexos):
   - `pnpm --filter @cortexos/dashboard-next exec tsc --noEmit` → exit 0
   - `bash -c 'set -a; source /opt/cortexos/.secrets/dashboard.env; set +a; cd packages/dashboard-next && pnpm exec vitest run'`
     → zero failures; quote the new total AND the pre-deletion total
     (capture it in Task 1 with the same command); the delta must equal
     exactly the tests in [discovered-test:] files (0 if none);
   - `pnpm --filter @cortexos/dashboard-next build` → exit 0
   - `pnpm exec eslint . >/tmp/z.txt 2>/dev/null; echo rc=$?` → rc=0 AND
     `wc -c < /tmp/z.txt` → 0
   - `pnpm run format:check >/dev/null 2>&1; echo rc=$?` → rc=0
     (pipe-safe; no tail masking)
   - the runtime-mock-import survivor count:
   `grep -rn '^import { api } from "@/mocks/api"' packages/dashboard-next/src --include='*.ts*' | grep -v __tests__ | grep -cv 'src/mocks/'` → 0
   (anchored to real import lines — doc comments excluded; TopBar's
   mock import was swapped to the live client pre-dispatch)
   (the LAST runtime mock imports leave with these four);
   - LIVE-ADMIN SURVIVAL (binary): `ls packages/dashboard-next/src/routes/ | grep -c 'admin'`
     → 8 (account, alerts, badges, env-browser, projects, services,
     users + the _authenticated.admin.tsx layout);
     `grep -c 'services.functions' packages/dashboard-next/src/features/admin/rpc.ts`
     → ≥1 (the live rpc seam intact).
   NOTE (orchestrator-owned, NOT a worker gate): post-deploy screen
   verification happens AFTER merge+deploy via the harness wp-b recon
   job (run 24; prompt pattern prompts/recon-wp-b-run23.md). Binary
   definition: the run-24 artifact reports 18/18 PASS, and its
   /admin/services and /admin/env-browser rows are PASS. The worker
   does not run, deploy, or screenshot anything.
4. ONE commit:
   chore(dashboard-next): retire the four mock admin duplicates — docker/incus/systemd/audit (live main pages cover them) (MP-028)
   Do NOT stage .planning/**. NEVER push.
End IMPL-COMPLETE or IMPL-BLOCKED: <reason>.

## Out of scope
- The LIVE admin pages (Users/Badges/EnvBrowser/Services/Account/
  Alerts/Projects) — they stay; src/mocks/** (test fixtures);
  NavConfig.ts (no entries reference the deleted routes — proof 1b).
