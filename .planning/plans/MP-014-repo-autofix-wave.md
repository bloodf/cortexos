# MP-014 — repo-wide autofix wave: prettier --write + eslint --fix (no manual edits)

## Requirements
- MP14-R1: true post-artifact-ignore inventory
  (`.planning/harness/artifacts/impl-mp-013-report.md:133-147`, MP-013b
  re-run, IMPL-COMPLETE): lint `3273 problems (3140 errors, 133
  warnings)` (:138) with `937 errors and 3 warnings potentially fixable
  with the --fix option` (:139); format `Code style issues found in 69
  files` (:147). Operator approved FULL CLEANUP
  (`.planning/GATE-RESOLUTION.md:434-444`); this plan is its first
  (mechanical) wave.
- MP14-R2: `packages/dashboard-next/src/routeTree.gen.ts` is GENERATED —
  its own header banner (lines 7-8) reads "This file was automatically
  generated by TanStack Router. You should NOT make any changes in this
  file as it will be overwritten" (reproduce:
  `head -8 packages/dashboard-next/src/routeTree.gen.ts`). It appears in
  the 69 unformatted files; generated files get ignored, not formatted:
  add `**/*.gen.ts` to `.prettierignore` BEFORE running prettier.
- MP14-R3: behavior must not change. Guards: every audited package's own
  gates re-run green afterward (the audit table at
  `.planning/harness/artifacts/recon-monorepo-audit.md` §5 lists which
  packages have which gates), plus dashboard-next's full gates and the
  live-service boot check stays orchestrator-owned.

ALL commands run from `/opt/cortexos`. NO manual source edits — only the
two tools' automatic rewrites plus the single `.prettierignore` line.

## File ownership
- `.prettierignore` (one line, per MP14-R2)
- Any file that `prettier --write` or `eslint --fix` itself modifies.
- Report (written, never committed):
  `/opt/cortexos/.planning/harness/artifacts/impl-mp-014-report.md`

## Tasks (append to the report after EVERY step)
1. BASELINE (quote): `pnpm lint 2>&1 | tail -2` and
   `pnpm run format:check 2>&1 | tail -2`.
2. Add `**/*.gen.ts` to `.prettierignore` (match existing style).
3. `pnpm exec prettier --write . 2>&1 | tail -3` (quote).
4. `pnpm exec eslint . --fix 2>&1 | tail -2` (exit may be non-zero —
   manual residue remains by design; quote).
5. Re-inventory (quote): `pnpm lint 2>&1 | tail -2` →
   expected total ≈ 3273 − ~940; `pnpm run format:check 2>&1 | tail -2` →
   expected exit 0. Then per-top-level-directory lint counts (MP13-R2
   method).
6. Package gates, ALL quoted with exit codes:
   - `pnpm --filter @cortexos/contracts build && pnpm --filter @cortexos/contracts test`
   - `pnpm --filter @cortexos/audit test` (or its script name per its
     package.json — use what the audit table ran)
   - `pnpm --filter @cortexos/mail-guardian build && pnpm --filter @cortexos/mail-guardian test`
   - `pnpm --filter @cortexos/telemetry test`
   - `pnpm --filter @cortexos/paperclip-adapter build && pnpm --filter @cortexos/paperclip-adapter test`
   - `node --check packages/cortex-terminal/src/server.js`
   - dashboard-next: `pnpm --filter @cortexos/dashboard-next exec tsc --noEmit`;
     full suite `bash -c 'set -a; source /opt/cortexos/.secrets/dashboard.env; set +a; cd packages/dashboard-next && pnpm exec vitest run'`
     (zero failures ≥ 558); `pnpm --filter @cortexos/dashboard-next build`.
   ANY failure → STOP, quote it, end IMPL-BLOCKED (do not chase it).
7. ONE commit of `.prettierignore` + all tool-modified files:
   style: repo-wide autofix wave — prettier --write + eslint --fix, no manual edits (MP-014)
   Do NOT stage .planning/**. NEVER push.
8. Acceptance summary + SHA; end IMPL-COMPLETE or IMPL-BLOCKED: <reason>.

## Acceptance (binary)
- A1: baseline and post-wave inventories quoted; format:check exit 0;
  lint total reduced by ≥ 900.
- A2: every Task-6 gate quoted green (or the wave is IMPL-BLOCKED at the
  first failure with nothing pushed).
- A3: no suppression/inline-config comments added:
  `git diff HEAD~1..HEAD | grep -cE '^\+.*(eslint-disable|eslint-enable|eslint-env|/\* *global |prettier-ignore)'` → 0.
- A4 (three independent binary checks):
  - `git diff HEAD~1..HEAD --numstat -- .prettierignore` shows exactly
    `1  0  .prettierignore` (one added line, zero removed), and
    `git diff HEAD~1..HEAD -- .prettierignore | grep -c '^\+\*\*/\*\.gen\.ts'`
    → 1 (the added line is exactly the planned pattern);
  - `git diff --name-only HEAD~1..HEAD | grep -c '^\.planning/'` → 0
    (nothing under .planning committed, closing the Task-7 enforcement
    gap the gate flagged);
  - `git diff --name-only HEAD~1..HEAD | grep -vc -E '^(packages/|stacks/|hermes|scripts/|templates/|docs/|prompts/|\.github/|\.prettierignore$|[^/]+\.(json|yml|yaml|md|js|ts|mjs|cjs)$)'`
    → 0 (every other changed path is repo content the two tools may
    touch — including `.github/` workflow YAML and `hermes-webui/`
    (matched by the `hermes` prefix), both present in the MP-013b format
    inventory — or the single ignore-file line).
- A5 (orchestrator, after): dashboard rebuild + restart + screen
  verification 18/18; per-package gates already covered by A2.

## Out of scope
- Manual lint fixes (next waves, sized from Task-5's new inventory).
- eslint/prettier config changes beyond the single ignore line.
- Generated-file regeneration; stacks/hermes runtime behavior.
