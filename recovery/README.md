# Recovery Scripts

This directory holds one-off repair or migration helpers that are not part of
the supported top-level `scripts/` surface.

Use a recovery script only when a troubleshooting document or operator runbook
explicitly calls for it. New install and validation prompts should prefer the
canonical runtime path and `scripts/cortex-production-readiness.sh`.
