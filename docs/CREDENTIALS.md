# Credentials

Use dedicated machine or project identities for agents.

Credential placement is governed by:

- `manifests/rebuild/secrets.manifest.tsv`
- `manifests/rebuild/projects.tsv`
- `PLAN.md`

Personal GitHub credentials should not be installed into project instances.
