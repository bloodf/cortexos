# RHEL family support (RHEL / Rocky / AlmaLinux)

Filled in P6. RHEL/Rocky/Alma share the dnf family branch from `scripts/pkg.sh`. Differences vs Fedora documented in P6: subscription-manager (RHEL only), CRB+EPEL repos, missing `dnf module` streams on RHEL 9 for some packages.
