# Contributing

Use `AGENTS.md` as the current development contract.

Before changes:

```bash
pnpm install
pnpm lint
pnpm format:check
```

For commits, keep the repository's Lore commit protocol from `AGENTS.md`.
Do not add new runtime systems unless their placement, secrets, backup scope,
validation gate, and dashboard catalog entry are declared in `docs/`.
