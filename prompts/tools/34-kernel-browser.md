# 34 - Kernel Browser

Kernel Browser remains a host tool if needed by shared AI harnesses.

Validation:

```bash
curl -fsS http://127.0.0.1:9222/json/version
```

Continue with the sandbox runner and AgentGateway rebuild prompts.
