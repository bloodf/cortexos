# 25 - Node Exporter

Node Exporter remains part of host monitoring.

Validation:

```bash
curl -fsS http://127.0.0.1:9100/metrics >/dev/null
```

Continue with `31-9router.md` after the monitoring baseline is green.
